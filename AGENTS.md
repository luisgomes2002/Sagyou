# AGENTS.md

**Leia o `GUIDE.md` na raiz deste repositório antes de mexer no código.** Ele tem
a stack, a estrutura, as convenções, os comandos e — o que mais importa — as
regras cuja quebra corrompe dados reais de usuários.

Este arquivo é só um ponteiro: existe porque agentes de código procuram por
`AGENTS.md`, e o conteúdo mora em um lugar só (`GUIDE.md`) para não haver duas
versões da verdade.

Fonte da verdade sobre arquitetura, em detalhe: `CLAUDE.md`.
